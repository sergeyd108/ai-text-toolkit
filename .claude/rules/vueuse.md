# VueUse

Prefer `@vueuse/core` composables over writing custom implementations.

Before creating a custom composable, check if VueUse already provides one (e.g. `useClipboard`, `useLocalStorage`, `useDebounceFn`, `useBreakpoints`, `useMediaQuery`, `useEventListener`, etc.).
